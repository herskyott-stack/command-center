# runs/

Timestamped log of **every automated action**. One file per run: `YYYY-MM-DD-HHMMSS-short-name.md`.

Each entry records: what triggered it (voice command, schedule, webhook), what it did, what changed (commits, deploys), and the outcome. If it didn't get logged here, it didn't happen.
